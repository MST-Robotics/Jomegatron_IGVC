#ifndef MIDGPACKETGPSCLOCK_H
#define MIDGPACKETGPSCLOCK_H

struct msg_GPSCLOCK
{
    double timestamp; // in seconds, .01 precision
    double clock_bias; // in ns
    long clock_drift; // in ns/s
    unsigned long time_accuracy_estimate; // in ns
    unsigned long frequency_accuracy_estimate; // in ps/s
};

#endif
